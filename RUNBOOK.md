# RUNBOOK：San9AutoDomestic 操作手册

> 给人看的，不给代理看。代理侧规则在 `AGENTS.md`。
> 你需要记住的只有一句：一次开一个阶段的 goal，实机场次你必须在场。

---

## 一、一次性准备（做一次，五分钟）

### 1. 放文件

四份文件全部放仓库根目录 `C:\codex files\San9AutoDomestic\`：

```
AGENTS.md        Codex 自动读，规则
STATE.md         当前状态，覆盖式维护
PLAN-3M.md       阶段划分与预算
RUNBOOK.md       本文件，只有你读
```

不要放进 `docs\`。放 docs 里会跟旧逆向文档混在一起被一起读走。

### 2. 归档旧文档

这一步不做，前面省下的开场词元会原样长回来。

```powershell
cd 'C:\codex files\San9AutoDomestic'
mkdir docs\archive
move docs\CLAUDE_HANDOFF_2026-08-09.md docs\archive\
move .\CLAUDE_NATIVE_HANDOFF_2026-08-11.md docs\archive\
```

`PRD.md`、`VERIFICATION.md`、`reverse-engineering-v3/v7/v9/v10.md`、`easy-compatibility-p0.md` 留在原地不动，靠 AGENTS.md 的规则挡住全文读取。

### 3. 装工具

x32dbg（32 位版，游戏是 x86）。S3 和 S4 会用到，提前装好。

### 4. 存档副本

在游戏存档目录建一个工作副本，之后所有实机测试只在副本上跑。原档永远不碰。

做完这四步，S0 就完成了，零词元消耗。

---

## 二、每个阶段的固定流程

八个阶段全都走同一个循环，记住这个循环就够了：

1. 你开一个 goal，只覆盖当前一个阶段，把提示词复制进去。
2. 代理离线干活（写 C、离线构建、PE 审计、恶意参数矩阵），这部分不需要你，也不需要开游戏。
3. 代理停下来，说需要实机。
4. 你按第四节的清单准备环境，开游戏。
5. 你复制第五节对应的授权文本发给它。
6. 它跑，你看结果。失败就重启游戏再来，bridge 卸不掉，没有第二种回滚方式。
7. 阶段通过后，你按第六节做收口，再开下一个 goal。

代理不会替你开游戏，这是硬约束。它到了实机节点会停住等你。

---

## 三、什么时候需要开游戏

| 阶段 | 开游戏 | 你在场 | 大致时长 |
|---|---|---|---|
| S0 归档瘦身 | 否 | 五分钟 | 五分钟 |
| S1 probe0 实现 | 否 | 否 | 代理离线跑 |
| S1 实机验收 | **是** | **是** | 10 到 30 分钟，会重启游戏数次 |
| S2 ping 实现 | 否 | 否 | 代理离线跑 |
| S2 ping 验收 | **是** | **是** | 10 到 20 分钟 |
| S3 观测层实现 | 否 | 否 | 代理离线跑 |
| S3 录制手动商业 | **是** | **是，且你要亲手在游戏里点一次商业** | 30 到 60 分钟 |
| S4 城市绑定裁决 | **是**（x32dbg 下断点） | **是** | 数次断点场次 |
| S5 单城 Commerce | **是** | **是** | 多次，这是最长的阶段 |
| S6 四命令 | **是** | **是** | 每类命令各一次闭环 |
| S7 全城批次 | **是** | **是** | |
| S8 UI 与发布 | **是** | **是** | 20 轮批次回归 |

七到八成的词元花在不需要开游戏的离线实现上。实机场次短而密集。

---

## 四、实机场次准备清单

每次代理说需要实机，你照这个单子过一遍：

- [ ] 存档已复制，本次只用副本
- [ ] 游戏已启动，`San9PKEasy.exe` 注入完成
- [ ] 已进入有直属城市的剧本，且当前城市有可执行的商业
- [ ] Hard、SanIX 及其他所有修改器已关闭
- [ ] 没有第二个游戏实例，没有第二个 Easy 实例
- [ ] 记下当前前台窗口和鼠标位置（用来证明没有输入副作用）
- [ ] S3/S4 场次：x32dbg 已就绪

跑之前心里有数：bridge 装上就卸不掉，任何失败的恢复手段只有重启游戏。一个场次里重启五到十次是正常的。

---

## 五、可直接复制的文本

### 5.1 各阶段 goal 开场提示词

**S1（上限 120,000）**

```
读仓库根 STATE.md 和 PLAN-3M.md，遵守 AGENTS.md。
本次 goal 只做 S1（probe0），上限 120,000 词元。
工作面仅限 native/San9BridgeP1EasyPingM2b。
退出条件：DLL 真正驻留游戏进程，共享内存计数器随 idle 递增；或给出明确阻断。
不要写新文档，不要碰已冻结目录，不要扩 P1Wire schema，不要自己启动游戏。
离线部分完成后停下来等我开游戏并授权。
中途只汇报两次：离线构建首次全绿时，准备实机等授权时。
```

**S2（上限 130,000）**

```
读 STATE.md 和 PLAN-3M.md，遵守 AGENTS.md。本次 goal 只做 S2（ping-only），上限 130,000。
复用已冻结的 P1Wire，不改 schema。
退出条件：100/100 认证往返，主线程身份全程一致，游戏业务状态零变化。
其余同 S1。
```

**S3（上限 300,000）**

```
读 STATE.md 和 PLAN-3M.md，遵守 AGENTS.md。本次 goal 只做 S3（只读观测层 + 录制 + ABI 闭合），上限 300,000。
bridge 必须以 S3_READONLY_BUILD=1 编译，opcode 分发表中不得存在任何写入项。
读取 opcode 只接受编译进 DLL 的 (符号, 偏移, 长度) 三元组索引，单次上限 256 字节。
退出条件：拿到我手动执行一次商业的完整轨迹，并据此确定 PersonList 尺寸与 handler 布局。
需要断点的问题给我断点清单，我在 x32dbg 里跑，不要做静态反汇编推理。
```

**S4（上限 120,000）**

```
读 STATE.md 和 PLAN-3M.md，遵守 AGENTS.md。本次 goal 只做 S4（城市绑定可行性裁决），上限 120,000。
退出条件：对 cityId/CCityData* -> domestic controller target 给出明确 GO 或 NO-GO。
判 NO-GO 时直接在 STATE.md 里改产品定义为当前城市模式，不要绕开去做别的。
不要开始实现全城批次。
```

**S5（上限 550,000）**

```
读 STATE.md 和 PLAN-3M.md，遵守 AGENTS.md。本次 goal 只做 S5（单城 Commerce），上限 550,000。
顺序固定：no-apply 时序观测 → 单独申请业务写入授权 → 一次 apply → 存档重载验证。
只做当前直属城市，只提交一次，不循环，不第二城，不其他命令，不接 UI。
业务写入前必须停下来单独向我要授权。
```

**S6（450,000）/ S7（350,000）/ S8（200,000）** 照 S5 格式改目标和上限，退出条件抄 `PLAN-3M.md` 的表。

### 5.2 第二级授权文本（probe0 / ping / 观测 / no-apply 时序）

```
授权本批次进程内驻留测试。只做主线程计数、认证 ping 和只读内存观测，
不写入任何游戏业务字段。接受 bridge 固定驻留至游戏重启，测试结束后我会重启游戏。
当前使用复制存档。
```

### 5.3 第三级授权文本（业务写入 apply）

```
授权本批次执行一次原生商业命令的业务写入。仅限当前直属城市，仅一次 apply，
不循环，不做第二城，不做其他命令。当前使用复制存档。
接受结果不确定时锁止并重启游戏，不自动重试。
```

第二级授权不自动沿用到第三级。每个新阶段的第三级写入都要重新批。

---

## 六、阶段通过后的收口

阶段跑通了，你做三件事，然后才开下一个 goal：

1. 让代理把实机运行日志贴进 `STATE.md`，并把当前阻断那一节改成下一阶段的目标。
2. 让它更新 `STATE.md` 末尾的预算账本：已用、剩余、当前阶段。
3. 首次 live 之前建一个 checkpoint commit，只纳入已核验范围，不要 `git add .`。

阶段没跑通就烧完预算的情况，不要直接追加。让它在 `STATE.md` 里写一段不超过 200 字的复盘，说明卡在哪，然后你决定是否从 700,000 储备里放一笔。储备是按次释放的，不是自动续期。

---

## 七、什么时候该喊停

看到下面任何一条，立刻打断代理：

- 它开始新建 markdown 文件，或者要写第 11 版逆向文档
- 它在读 `docs/archive/` 或者整篇读 v3/v7/v9/v10
- 它提议扩 P1Wire schema，或者新建一套平行协议、平行状态机
- 它在 C# 侧重新实现进程发现或 wire 编解码
- 阶段预算用到 40%，还没有一次真实运行
- 它自己去启动游戏或 Easy
- 它把离线测试全绿当成阶段结论
- 它同时推进两条技术路线
- 同一个问题进入第三轮复审
- 它用 `git reset --hard` 或 checkout 覆盖工作树

前四条是这个项目过去两次烧穿预算的直接原因，出现即打断，不要等它自己收敛。

---

## 八、一句话

先让 DLL 进游戏并让计数器动起来，再跑 100 次 ping，再录一次你自己手点的商业。三件事做完，剩下的都是照着轨迹填空。

