# AGENTS.md

本仓库的代理工作规则。开工前必读，规则优先级高于任何历史文档。

## 开场顺序

1. 读根目录 `STATE.md`（约 1,900 词元）。它是当前状态的唯一真源。
2. 读根目录 `PLAN-3M.md`。它是阶段划分、预算与执行规则。
3. 开始干活。

**不要**在开场读 `docs/PRD.md`、`docs/VERIFICATION.md`、`docs/reverse-engineering-v*.md`、`docs/easy-compatibility-p0.md`。这些是按需查的参考件，只允许 grep 定位后读片段。

**不要**读 `docs/archive/` 下的任何文件。那里是已作废的历史交接文档，内容与当前状态冲突。

**不要**读 `RUNBOOK.md`。那是给人类操作者看的手册，与代理无关。

## 硬约束

**文档配额**：整个剩余项目文档写作总量 ≤ 60,000 词元。只允许写进 `STATE.md`（覆盖式，改现有内容，不追加章节）和每阶段一份 ≤ 200 行的 `NOTES-S<n>.md`。禁止新建 `reverse-engineering-v11` 及之后的编号文档。禁止再写交接文档。禁止把分析结论写成新的 markdown 文件。

**冻结目录**：以下目录禁止修改，只读引用。

- `native/San9BridgeP1Wire`
- `prototypes/San9AutoDomestic.P1Wire`
- `native/San9BridgeP1EasyPing`
- `native/San9BridgeP1EasyPingM2`
- `docs/easy-compatibility-manifest.json`

禁止扩展 P1Wire schema。禁止新增第二份 wire 协议实现。业务 opcode 走 M2a 已有的 4096 字节 mailbox。

**语言边界**：进程发现、身份绑定、哈希校验的唯一真源在 C 侧。C# 侧不实现任何 wire 编解码，不重复实现进程发现。UI 与 native 的唯一接口是启动 `controller.exe --inspect` 读 stdout JSON。

**Git**：禁止 `git reset --hard`、`git checkout` 覆盖工作树。当前工作树非 clean 且大量目录未跟踪，覆盖会丢失未提交实现。以文件哈希和可重复构建为证据，不以 HEAD 名称推断功能。

## 进度定义

唯一的进度指标是跑起来了什么。测试数量、文档篇幅、审计轮次都不算进度。

阶段结束条件必须是一次真实运行的日志，贴进 `STATE.md`。离线测试全绿不构成阶段结束。

## 止损

阶段预算用到 40% 仍无一次真实运行，立刻停止扩设计，把剩余全部转到最小可运行片。

同一个问题最多一次实现加一次复审。第二次复审仍不通过，直接上真机跑，让实跑当审计。S1 到 S3 不设独立审计线程。

禁止同阶段并行两条技术路线。禁止多层代理树，每阶段最多一个实现线程。

## 遇到未知 ABI 事实

先问一句：能不能用一个断点在两分钟内看到。

能，就产出断点清单（地址、触发条件、要记录的寄存器和内存区间），停下来交给用户在 x32dbg 里跑，等他贴回结果。不要做静态反汇编推理。

不能，才做静态分析，且单个问题上限 20,000 词元。

## Live 运行

绝不自行启动游戏或 San9PKEasy。启动顺序是硬约束：游戏 → Easy 注入完成 → 助手验证 → 助手装桥。剧本、直属城市和存档由用户选定。

授权分三级，见 `STATE.md`：

1. 只读 inspect，无需单独授权。
2. 进程内驻留加只读 opcode，覆盖 probe0、ping、观测录制、no-apply vptr CAS 时序。需用户明确授权。
3. 业务写入 apply。需另行单独授权。

上一级授权不自动沿用到下一级。任何 `AbortUncertain` 之后必须重启游戏，不在同一进程代继续试。

到达需要实机的节点时停下来，向用户说明：要跑什么、预期观察到什么、失败如何回滚。然后等待。不要为了推进而把 live 预算转成更多静态研究。

## S3 只读构建

S3 的 bridge 必须以 `S3_READONLY_BUILD=1` 编译。该构建下：

- opcode 分发表中物理上不存在任何写入项。
- 读取 opcode 只接受编译进 DLL 的 (符号, 偏移, 长度) 三元组索引，不接受裸指针。
- 单次读取上限 256 字节。
- PE 审计增加一条：分发表条目数与只读标记一致。

## 当前阶段

见 `STATE.md` 末尾的预算账本。只做当前阶段，不预先实现下一阶段的内容。

